<core-header @routeMode={{routeMode}}></core-header>

<div class="news-view">
  {{#if (noteq routeMode 'item')}}
    {{#if (noteq routeMode 'user')}}
      <div class="news-list-nav">
        {{#if (noteq page 1)}}
          <a onclick={{action previousPage}}>&lt; Prev</a>
        {{/if}}
        {{page}}
        {{#if results.length}}
          <a onclick={{action nextPage}}>Next &gt;</a>
        {{/if}}
      </div>
    {{/if}}
  {{/if}}
  <div class="news-list">
    <div class="view">
      {{#if loading}}
        <loading-sign></loading-sign>
      {{else}}
        {{#if (eq routeMode 'item')}}
          <comments-list @comments={{comments}} @post={{post}}></comments-list>
        {{/if}}
        {{#if (noteq routeMode 'item')}}
          {{#if (noteq routeMode 'user')}}
            <items-list @results={{results}}></items-list>
          {{/if}}
        {{/if}}
        {{#if (eq routeMode 'user')}}
          <user-info @userInfo={{userInfo}}></user-info>
        {{/if}}
      {{/if}}
      <core-footer></core-footer>
    </div>
  </div>
</div>

